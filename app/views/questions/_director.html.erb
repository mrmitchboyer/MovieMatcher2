<div class="panel panel-default col-md-8 col-md-offset-2 question text-center">
  <h2>Do you like any of these directors?</h2>
  <div class="panel-body">
    <div class="multiple-alert">
    <div class="arrow-left"></div>
    <div class="blue-box text-left"><em>You can choose multiple answers</em></div>
  </div>
    <%= hidden_field_tag "ans[director][name]", :director %>
    <div class="col-sm-4 col-sm-offset-4">
      <p>
        <% @directors.each do |director| %>
          <div class="checkboxes text-left">
            <%= check_box_tag "ans[director][answer][]", director %>
            <%= label_tag "ans[director][answer][]", director %><br>
          </div>
        <% end %>
      </p>
    </div>
    <div class="clearfix"></div>
    <div>
    <h4>How much do you care about the movie's director?</h4>
      <p>
        <% Question.weight.each do |label, weight| %>
          <%= radio_button_tag "ans[director][weight]", weight %>
          <%= label_tag "ans[director][weight]", label %>
        <% end %>
      </p>
    </div>
    <a class="btn btn-primary new-question blue-glow btn-animation">Ask Another Question</a>

  </div>
</div>